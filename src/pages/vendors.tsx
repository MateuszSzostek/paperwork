import React from 'react'

import Layout from "../components/Layout"

const Vendors = ({children}) => {
    return (
        <Layout>
            {children}
            THIS IS VENDORS COMPONENT
        </Layout>
    )
}

export default Vendors
