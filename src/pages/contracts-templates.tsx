import React from 'react'

import Layout from "../components/Layout"

const ContractsTemplates = ({children}) => {
    return (
        <Layout>
            {children}
            THIS IS CONTRACTS TEMPLATES COMPONENT
        </Layout>
    )
}

export default ContractsTemplates
